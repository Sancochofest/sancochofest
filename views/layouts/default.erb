<!DOCTYPE HTML>
	<head>

	     <%= partial(:'partials/head') %>

	     <%= yield_content :head %>

	</head>
	<body>

	   <%= partial(:'partials/header') %>

	   <%= partial(:'partials/main-menu') %>

	   <%= yield %>
	</body>
</html>
